---
version: 3

images:
  base_image:
    name: quay.io/centos/centos:stream9

additional_build_steps:
  prepend_base: |
    RUN echo "hello from prepend_base" > /baseout.txt

options:
  package_manager_path: '/bin/true'
  skip_ansible_check: yes
